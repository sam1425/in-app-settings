this are the most used shortcuts for text editors
so
`something`
* `ctrl+/` is to comment with //
* `ctrl+shift+/` 
* `ctrl+f` find
* `ctrl+shift+f` find(all  proyect)
* `ctrl+enter` adds a new line without any distub
* `ctrl+p` search for thing